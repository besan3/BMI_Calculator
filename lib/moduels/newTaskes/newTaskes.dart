import 'package:flutter/material.dart';
class NewTaskes extends StatelessWidget {
  const NewTaskes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
     child: Center(child: Text('New Taskes'))
    );
  }
}
